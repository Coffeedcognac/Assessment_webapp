name: hello world
 
on:
  push:
    branches:
      - main
 
jobs:
  build:
    name: Build
    runs-on: [self-hosted]
    steps:
      - uses: actions/checkout@v4
      - name: Install App Dependencies
        run: node index.js
